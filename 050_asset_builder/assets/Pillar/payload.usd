#usda 1.0
(
    defaultPrim = "Pillar"
    framesPerSecond = 24
    metersPerUnit = 1
    timeCodesPerSecond = 24
    upAxis = "Y"
)

def "Pillar" (
    kind = "component"
    prepend references = [
        @./mtl.usd:SDF_FORMAT_ARGS:format=usda@,
        @./geo.usd@
    ]
)
{
}

